<html ng-app='app'>
<head>
<title></title>
<link rel="stylesheet" href="https://ajax.googleapis.com/ajax/libs/angular_material/1.1.12/angular-material.min.css">
</head>
<body>

<div layout='col' ng-controller='secondCtrl'>
	<form>
       <label>Text: <input type='text' ng-model='_name'/></label><br />
       <label>Price: <input type='number' ng-model='price'/></label><br />
       <select ng-model="selectedType">
       	<option ng-repeat="typ in types" value="{{typ.nm}}">{{ typ.nm }}</option>
       </select>
       <input type="submit" ng-click='add(a=_name, b=price, c=selectedType)'>
   </form>
</div>
<div layout='col' ng-controller='firstCtrl'>
	<select 
	ng-model="sorting"
	md-on-close="sortList(sorting)"
	>
       	<option ng-repeat="typ in myFactory.types" value="{{typ.nm}}">{{ typ.nm }}</option>
   	</select>
	<ul style="list-style-type:none">	
		<div ng-repeat="val in current_list"> 
			<li id="{{ 'id-'+val.thisname  }}">
				<p>🏇comment🏇: {{ val.thisname }} 🏇<button onclick="">🏇</button></p>
				<p>🏇price🏇: {{ val.thisprice }} 🏇</p>
				<p>🏇type🏇: <b>{{ val.thistype }} </b>🏇</p>
				
			<li>
		</div>
	</ul>
</div>
<script src='https://ajax.googleapis.com/ajax/libs/angularjs/1.7.9/angular.min.js'></script>
<script src="" >

</script>
<script type='text/javascript' src='main.js'></script>
</body>
</html>